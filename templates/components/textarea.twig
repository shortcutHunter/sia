<tr>
	<td>
		<div class="text-muted font-semibold me-3 {{ required ? 'required' : '' }}">{{ label }}</div>
	</td>
	<td>
		<textarea type="text" ng-model="vm.data.{{ name }}" class="form-control" {{ required ? 'required' : '' }}></textarea>
	</td>
</tr>
