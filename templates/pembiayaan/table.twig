{% extends "structure/table.twig" %}

{% block button %}
	<a ui-sref="pembiayaan_form({dataId: null})" class="btn btn-success btn-sm">
		<i class="fa fa-plus"></i> Tambah
	</a>
{% endblock %}

{% block table %}

	{% verbatim %}

		<thead>
			<tr>
				<th>Kode</th>
				<th>Nama</th>
				<th>Nominal</th>
			</tr>
		</thead>

		<tbody>
			<tr ng-if="!vm.data || vm.data.length == 0">
	          <td colspan="5" class="text-center">Tidak ada data yang tersedia</td>
	        </tr>
	        <tr ng-repeat="data in vm.data" class="clickable" ng-attr-tr_id="{{data.id}}" state_name="pembiayaan_detail" widget-tr="">
	          <td ng-bind="data.kode || '-'"></td>
	          <td ng-bind="data.nama || '-'"></td>
	          <td ng-bind="data.nominal || '-'"></td>
	        </tr>
		</tbody>

	{% endverbatim %}
{% endblock %}