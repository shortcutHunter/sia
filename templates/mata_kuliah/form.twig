{% extends 'structure/form.twig' %}


{% block content %}
		
	<div class="row">
		<div class="col-md-6">
			<table class="custom-table">
				{% include 'components/input.twig' with {'name': 'kode', 'label': 'Kode'} %}
				{% include 'components/input.twig' with {'name': 'nama', 'label': 'Nama'} %}
				{% include 'components/input.twig' with {'name': 'sks', 'label': 'SKS'} %}
			</table>
		</div>

		<div class="col-md-6">
			<table class="custom-table">
				{% include 'components/checkbox.twig' with {'name': 't', 'label': 'Teori'} %}
				{% include 'components/checkbox.twig' with {'name': 'p', 'label': 'Praktek'} %}
				{% include 'components/checkbox.twig' with {'name': 'k', 'label': 'SKS'} %}
			</table>
		</div>

		<hr>

		<div class="col-md-6">
			<table class="custom-table">
				{% include 'components/textarea.twig' with {'name': 'keterangan', 'label': 'Keterangan'} %}
			</table>
		</div>
	</div>

{% endblock %}